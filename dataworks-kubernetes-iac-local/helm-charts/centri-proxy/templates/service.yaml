apiVersion: v1
kind: Service
metadata:
  name: {{ include "centri-proxy.fullname" . }}
  labels:
    {{- include "centri-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "centri-proxy.selectorLabels" . | nindent 4 }}
