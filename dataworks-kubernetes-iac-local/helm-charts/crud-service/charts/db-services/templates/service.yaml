apiVersion: v1
kind: Service
metadata:
  name: {{ include "db-services.fullname" . }}
  labels:
    {{- include "db-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "db-services.selectorLabels" . | nindent 4 }}
