apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "indexingservice.fullname" . }}-test-connection"
  labels:
    {{- include "indexingservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "indexingservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
