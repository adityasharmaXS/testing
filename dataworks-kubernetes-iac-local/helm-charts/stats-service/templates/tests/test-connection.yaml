apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stats-service.fullname" . }}-test-connection"
  labels:
    {{- include "stats-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stats-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
