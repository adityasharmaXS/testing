apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "outputfactory.fullname" . }}-test-connection"
  labels:
    {{- include "outputfactory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "outputfactory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
