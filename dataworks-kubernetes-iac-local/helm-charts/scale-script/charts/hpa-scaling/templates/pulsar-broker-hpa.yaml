apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "hpa-scaling.fullname" . }}-pulsar-broker
  namespace: dataworks-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: pulsar-broker
  minReplicas: {{ .Values.pulsar.broker.minReplicas }}
  maxReplicas: {{ .Values.pulsar.broker.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: 150
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: 350
