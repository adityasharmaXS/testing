apiVersion: v1
kind: Service
metadata:
  name: {{ include "hpa-scaling.fullname" . }}
  labels:
    {{- include "hpa-scaling.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hpa-scaling.selectorLabels" . | nindent 4 }}
