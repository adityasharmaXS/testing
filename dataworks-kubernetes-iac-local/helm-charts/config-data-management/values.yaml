# Default values for config-data-management.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: dwproductioncentralus.azurecr.io/crud-management
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "112"
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""
podAnnotations: {}
podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
# capabilities:
#   drop:
#   - ALL
# readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
  port: 80
  grpcPort: 53001
resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 1000m
    memory: 1024Mi
  requests:
    cpu: 100m
    memory: 128Mi
env:
  AZURE_STORAGE_ACCESS_KEY: "zh8ja4GxMXENVYMs9zeR5bP67SH/nhvxC1A2ZUNKZ+9MQlQ6umoS4FyxTvdFHaueVFdFukEg7SJeLMMZMoMiBQ=="
  AZURE_STORAGE_ACCOUNT: "dwlakeus"
  JWT: "90G03KUhc5mYl2y300wAp7WGi2ue4"
  MONGO_DB: "merit"
  MONGO_DB_TEST: "merit_test"
  MONGO_HOST: "mongodb"
  MONGO_PORT: "27017"
  POSTGRES_DB: "dataworks"
  POSTGRES_DB_TEST: "datawork_test"
  POSTGRES_HOST: "postgres"
  POSTGRES_PASSWORD: "postgres"
  POSTGRES_PORT: "5432"
  POSTGRES_USER: "postgres"
  REDIS_HOST: "redis-master"
  REDIS_PORT: "6379"
  SESSION_KEY: "FR8zEL84bfR57d13psly7TCcHBJLb27c4P33v"
  PORT: "3001"
  GRPCPORT: "53001"
  PUBLIC_STORAGE: "/data/public-files"
  PRIVATE_STORAGE: "/data/private-files"
  TAXONOMIES_STORAGE: "/data/files"
  CSV_STORAGE: "/data/csv"
  ENV_STATUS: "prod"
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80
labels:
  contact: anish
  workload: config-data-management
  env: prod
  tier: backend
  project: dataworks
nodeSelector: {}
tolerations: []
affinity: {}
