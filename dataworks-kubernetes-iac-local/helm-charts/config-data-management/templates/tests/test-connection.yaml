apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "config-data-management.fullname" . }}-test-connection"
  labels:
    {{- include "config-data-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "config-data-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
