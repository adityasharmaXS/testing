apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jobfactory.fullname" . }}-test-connection"
  labels:
    {{- include "jobfactory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jobfactory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
