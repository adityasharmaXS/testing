apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workerbot.fullname" . }}-test-connection"
  labels:
    {{- include "workerbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workerbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
