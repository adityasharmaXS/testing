apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cache-service.fullname" . }}-test-connection"
  labels:
    {{- include "cache-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cache-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
