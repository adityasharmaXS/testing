apiVersion: v1
kind: Service
metadata:
  name: {{ include "fisson-function.fullname" . }}
  labels:
    {{- include "fisson-function.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fisson-function.selectorLabels" . | nindent 4 }}
