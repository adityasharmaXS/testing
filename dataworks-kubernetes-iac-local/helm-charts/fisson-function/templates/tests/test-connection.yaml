apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fisson-function.fullname" . }}-test-connection"
  labels:
    {{- include "fisson-function.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fisson-function.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
