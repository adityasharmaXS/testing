apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meta-writer.fullname" . }}-test-connection"
  labels:
    {{- include "meta-writer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meta-writer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
