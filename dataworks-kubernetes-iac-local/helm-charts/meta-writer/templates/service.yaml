apiVersion: v1
kind: Service
metadata:
  name: {{ include "meta-writer.fullname" . }}
  labels:
    {{- include "meta-writer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meta-writer.selectorLabels" . | nindent 4 }}
